import styled from 'styled-components';

export const ItemsList = (): JSX.Element => {
  const Box = styled.div`
    border: 1px solid black;
    margin: 0.5rem;
    width: 200px;
  `;

  return <Box>Items List</Box>;
};
